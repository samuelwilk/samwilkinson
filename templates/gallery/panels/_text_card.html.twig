{# Text card panel #}
<div class="w-full h-full flex items-center justify-end pr-24">
    <div class="bg-ink text-paper px-12 py-16 max-w-md">
        <h3 class="text-xs font-mono uppercase tracking-[0.3em] mb-6">
            {{ panel.title }}
        </h3>
        <p class="text-base leading-relaxed">
            {{ panel.body }}
        </p>
    </div>
</div>
