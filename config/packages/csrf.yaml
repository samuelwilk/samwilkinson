# Enable stateless CSRF protection for forms
# Note: authenticate uses session-based CSRF for better compatibility with Docker/local dev
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - logout
