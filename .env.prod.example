# Production Environment Configuration
# Copy to .env.prod and configure with real values

###> symfony/framework-bundle ###
APP_ENV=prod
APP_SECRET=CHANGE_ME_TO_RANDOM_64_CHAR_STRING
###< symfony/framework-bundle ###

###> Database Configuration ###
# SQLite (simple, zero-config)
DATABASE_URL="sqlite:///%kernel.project_dir%/var/data/database.sqlite"

# OR PostgreSQL (for higher traffic)
# DATABASE_URL="postgresql://db_user:db_password@db_host:5432/db_name?serverVersion=16&charset=utf8"

# OR MySQL (alternative)
# DATABASE_URL="mysql://db_user:db_password@db_host:3306/db_name?serverVersion=8.0.32&charset=utf8mb4"
###< Database Configuration ###

###> Security ###
# Trusted hosts (comma-separated, no spaces)
TRUSTED_HOSTS=^samwilkinson\.ca$,^www\.samwilkinson\.ca$

# Trusted proxies (if behind load balancer/CDN)
# TRUSTED_PROXIES=127.0.0.1,10.0.0.0/8
###< Security ###

###> Photo Storage ###
# Local filesystem (default)
PHOTO_STORAGE_ADAPTER=local
PHOTO_STORAGE_PATH=%kernel.project_dir%/public/uploads/photos

# OR Cloudflare R2 / S3-compatible
# PHOTO_STORAGE_ADAPTER=s3
# AWS_S3_ENDPOINT=https://[account-id].r2.cloudflarestorage.com
# AWS_S3_BUCKET=samwilkinson-photos
# AWS_ACCESS_KEY_ID=your_r2_access_key
# AWS_SECRET_ACCESS_KEY=your_r2_secret_key
# AWS_REGION=auto
# PHOTO_CDN_URL=https://cdn.samwilkinson.com
###< Photo Storage ###

###> mailer ###
# Configure if email notifications needed
# MAILER_DSN=smtp://localhost:1025
###< mailer ###

###> API Authentication ###
# Generate secure API tokens for iOS Shortcut bulk uploads
# Format: token_name:hashed_token (use bin/console app:generate-api-token)
# API_TOKENS=

###< API Authentication ###
